{
  "schemaVersion": 1,
  "id": "uuidfixer",
  "version": "${version}",
  "name": "UUID Fixer",
  "description": "Assigns Mojang UUIDs to players joining an offline-mode server, allowing in-game entities and other services (mods like Essential) to use the actual UUID instead of a hash of the player's username.",
  "authors": ["komixkat"],
  "contact": {
    "sources": "https://github.com/komixkat/uuidfixer",
    "issues": "https://github.com/komixkat/uuidfixer/issues"
  },
  "license": "MIT",
  "environment": "server",
  "entrypoints": {
    "main": ["dev.komixkat.uuidfixer.UuidFixerMod"]
  },
  "mixins": ["uuidfixer.mixins.json"],
  "depends": {
    "fabricloader": ">=0.19.0",
    "fabric-api": "*",
    "minecraft": "~26.2"
  }
}
